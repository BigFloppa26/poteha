---
servers:
- url: "http://localhost:8080"
openapi: "3.0.0"
paths:
  /{bucket}/{key}:
    $ref: "./minio.yaml#/paths/~1{bucket}~1{key}"
  /{bucket}:
    $ref: "./minio.yaml#/paths/~1{bucket}"
info:
  title: "merged spec"
  description: "merged spec"
  version: "1.0.0"
